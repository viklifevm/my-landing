# CLAUDE.md — Правила работы с проектом my-landing

## О проекте

Лендинг-портфолио **Виктории Музыченко** под брендом **AI Решатель**.
Один HTML-файл (`index.html`) — никаких фреймворков, сборщиков и внешних зависимостей.

Автор и заказчик — Виктория Музыченко (viklife@mail.ru, Telegram @VMuzychenko8).

---

## Структура файлов

| Файл | Назначение |
|---|---|
| `index.html` | Весь сайт — HTML + CSS + JS в одном файле |
| `photo.jpg.png` | Фото автора для hero-секции |
| `brief.md` | Техническое задание. Главный документ проекта — все решения по контенту и дизайну принимаются на его основе |
| `research.md` | Исследование конкурентов, трендов, экспертная оценка |

---

## Технический стек

- Чистый HTML5 + CSS3 + Vanilla JS
- Никаких npm, webpack, библиотек, CDN
- Файл должен открываться напрямую из браузера (`file://`) и из мессенджеров

---

## Дизайн-система

```css
--bg:      #0a0a0f   /* основной фон */
--bg2:     #111118   /* фон секций */
--bg3:     #1a1a24   /* карточки */
--accent:  #a78bfa   /* фиолетовый, основной акцент */
--accent2: #60a5fa   /* синий, вторичный акцент */
--text:    #f8f8f8
--text2:   #a0a0b0
--text3:   #606070
--success: #34d399
```

**Тема:** тёмная. Светлую не добавлять.

---

## Брендинг и имена

- **Бренд в навигации, заголовке страницы, футере:** `AI Решатель`
- **Личное имя в блоке "Обо мне" и копирайте:** `Виктория Музыченко`
- **Нигде не упоминать:** GitHub-профиль, LinkedIn
- **Контакты:** Telegram `@VMuzychenko8`, Макс `@VMuzychenko8`, email `viklife@mail.ru`

---

## Правила редактирования index.html

### Что нельзя трогать без явной просьбы
- Цветовую схему (CSS-переменные в `:root`)
- Порядок секций
- Контактные данные (email, телеграм, макс)
- Имена и бренд

### CSS
- Все стили — внутри `<style>` в `<head>`, один блок
- Не добавлять `!important` без нужды (уже есть только там, где необходимо перебить наследование)
- Анимации через CSS `@keyframes`, не через JS-библиотеки
- Новые правила добавлять в конец соответствующего смыслового блока, не в хаотичном порядке

### JS
- Весь JS — в одном `<script>` перед `</body>`
- Не использовать `document.write`, `eval`, `innerHTML` для пользовательского ввода
- Новые обработчики добавлять после существующих, в конец скрипта

### Мобильная совместимость
- Брейкпоинты: 1024px (планшет), 768px (мобильный), 480px (маленький экран)
- Проверять, что новые элементы корректно отображаются на 320px–480px
- Кнопки и ссылки — минимум 44px по высоте для удобного тапа

---

## Контент

- Все тексты — на русском языке
- Тон: уверенный, деловой, без жаргона и без излишней скромности
- Не менять позиционирование («экс-CEO, которая знает, где бизнес теряет деньги») без явного запроса

---

## Git

- Ветка: `master`
- Remote: `https://github.com/viklifevm/my-landing.git`
- Коммитить только по явной просьбе
- В коммит включать `index.html` и медиафайлы (фото и т.п.)
- `brief.md` и `research.md` коммитить только если они изменились намеренно
- Сообщения коммитов — на английском, кратко описывают суть изменений
